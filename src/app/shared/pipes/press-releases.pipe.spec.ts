import { PressReleasesPipe } from './press-releases.pipe';

describe('PressReleasesPipe', () => {
  it('create an instance', () => {
    const pipe = new PressReleasesPipe();
    expect(pipe).toBeTruthy();
  });
});
