import { ShowOnScrollUpDirective } from './show-on-scroll-up.directive';

describe('ShowOnScrollUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnScrollUpDirective();
    expect(directive).toBeTruthy();
  });
});
